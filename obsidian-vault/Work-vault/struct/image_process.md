Отлично! Мы нашли **`w1.g`** – это синглтон, отвечающий за обработку изображений (фильтры, блюр, рендеринг через RenderScript). Он **не занимается сетью**. Это значит, что зацепка из `MyApplication` (`w1.g.y().G()`) ведёт в графический модуль, а не в сетевой стек